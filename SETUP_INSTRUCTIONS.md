# Инструкция по настройке интеграции с блокчейном

## ✅ Что уже сделано

1. ✅ Добавлены типы для `LastSubmitted` в `src/types.ts`
2. ✅ Расширен `src/utils/storage.ts` для работы с `lastSubmitted`
3. ✅ Создан `src/config/contract.ts` с конфигурацией контракта
4. ✅ Создан `src/utils/contract.ts` с утилитами для работы с контрактом
5. ✅ Обновлен `ProfileModal` с кнопкой "Submit stats onchain"
6. ✅ Добавлена обработка ошибок и состояний загрузки

## 📋 Что нужно сделать

### 1. Установить ethers.js

```bash
cd c:\VibeCoding\Wordle
npm install ethers@^6.0.0
```

### 2. Развернуть контракт

Следуйте инструкциям в `C:\VibeCoding\Wordle-Contracts\DEPLOYMENT.md`:

```bash
cd C:\VibeCoding\Wordle-Contracts
npm install
npm run test
npm run deploy:base-sepolia  # Для тестовой сети
# или
npm run deploy:base  # Для mainnet
```

### 3. Настроить контракт в фронтенде

Откройте `src/config/contract.ts` и обновите:

```typescript
// Замените на адрес развернутого контракта
export const CONTRACT_ADDRESS = "0xВашАдресКонтракта";

// Установите правильный chainId:
// 84532 для Base Sepolia (тестовая сеть)
// 8453 для Base Mainnet
export const CONTRACT_CHAIN_ID = 84532; // или 8453 для mainnet
```

### 4. Протестировать

1. Запустите приложение: `npm run dev`
2. Сыграйте несколько игр
3. Откройте профиль (иконка пользователя)
4. Нажмите "Submit stats onchain"
5. Подтвердите транзакцию в кошельке
6. Проверьте транзакцию на BaseScan

## 🔍 Проверка работы

### Проверка конфигурации

Убедитесь, что в `src/config/contract.ts`:
- `CONTRACT_ADDRESS` не равен `0x0000000000000000000000000000000000000000`
- `CONTRACT_CHAIN_ID` соответствует сети развертывания

### Проверка в браузере

1. Откройте DevTools (F12)
2. Перейдите на вкладку Console
3. Проверьте, что нет ошибок при загрузке
4. При отправке статистики проверьте логи

### Проверка localStorage

После успешной отправки в `localStorage` должен появиться ключ `wordle-last-submitted`:

```json
{
  "games": 10,
  "wins": 7,
  "losses": 3,
  "nonce": 1,
  "txHash": "0x...",
  "timestamp": 1234567890
}
```

## 🐛 Troubleshooting

### Ошибка: "Cannot find module 'ethers'"
**Решение**: Выполните `npm install ethers@^6.0.0`

### Ошибка: "Кошелек не подключен"
**Решение**: Убедитесь, что пользователь подключил кошелек через Farcaster SDK

### Ошибка: "Неправильная сеть"
**Решение**: Приложение автоматически попытается переключить сеть. Если не получается, пользователь должен вручную переключиться на Base в кошельке.

### Ошибка: "Неверный nonce"
**Решение**: Обновите страницу и попробуйте снова

### Транзакция не проходит
**Решение**: 
1. Проверьте, что у пользователя есть ETH на Base для оплаты газа
2. Проверьте статус транзакции на BaseScan
3. Убедитесь, что контракт развернут и адрес правильный

## 📚 Дополнительная документация

- Подробная документация: `BLOCKCHAIN_INTEGRATION.md`
- Инструкция по развертыванию контракта: `C:\VibeCoding\Wordle-Contracts\DEPLOYMENT.md`
- README контракта: `C:\VibeCoding\Wordle-Contracts\README.md`
