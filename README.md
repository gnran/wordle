# Wordle - Игра "Угадай слово"

Полнофункциональная игра Wordle для браузера на React + TypeScript + Tailwind CSS.

## 🎮 Особенности

- **6 попыток** на угадывание слова из 5 букв
- **Цветовая индикация**:
  - 🟩 Зеленый - буква на правильной позиции
  - 🟨 Желтый - буква есть в слове, но не на позиции
  - ⬜ Серый - буквы нет в слове
- **Ввод через экранную и физическую клавиатуру**
- **Проверка слов по словарю**
- **Личный кабинет** со статистикой:
  - Общее количество игр
  - Количество побед и поражений
  - Процент побед
  - Текущая и лучшая серия побед
- **Сохранение прогресса** в localStorage
- **Восстановление состояния** после обновления страницы
- **Адаптивный дизайн** для мобильных устройств

## 🚀 Установка и запуск

### Требования

- Node.js 18+ 
- npm или yarn

### Шаги установки

1. **Установите зависимости:**
```bash
npm install
```

2. **Запустите dev-сервер:**
```bash
npm run dev
```

3. **Откройте браузер:**
Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist`.

## 📁 Структура проекта

```
Wordle/
├── src/
│   ├── components/        # React компоненты
│   │   ├── GameBoard.tsx      # Игровое поле
│   │   ├── Keyboard.tsx       # Виртуальная клавиатура
│   │   ├── StatsModal.tsx    # Модальное окно статистики
│   │   └── GameOverModal.tsx  # Модальное окно окончания игры
│   ├── data/
│   │   └── words.ts          # Словарь из 10 слов
│   ├── utils/
│   │   ├── gameLogic.ts      # Логика проверки слов
│   │   └── storage.ts         # Работа с localStorage
│   ├── types.ts              # TypeScript типы
│   ├── App.tsx               # Главный компонент
│   ├── main.tsx              # Точка входа
│   └── index.css             # Глобальные стили
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── tailwind.config.js
```

## 🎯 Как играть

1. Введите слово из 5 букв на русской раскладке
2. Нажмите "ВВОД" или клавишу Enter
3. Буквы окрасятся в цвета:
   - **Зеленый** - буква на правильной позиции
   - **Желтый** - буква есть в слове, но не на этой позиции
   - **Серый** - буквы нет в слове
4. У вас есть 6 попыток, чтобы угадать слово
5. Откройте статистику, нажав на иконку 📊 в правом верхнем углу

## 🔧 Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev-сервер
- **Tailwind CSS** - стилизация
- **localStorage** - сохранение данных

## 📝 Примечания

- Словарь ограничен 10 словами (как указано в требованиях)
- Все данные сохраняются в localStorage браузера
- Игра автоматически восстанавливает состояние при перезагрузке страницы
- Статистику можно сбросить через личный кабинет

## 🎨 Кастомизация

Вы можете легко расширить словарь, добавив слова в файл `src/data/words.ts`:

```typescript
export const WORDS = [
  'КОШКА',
  'ДОМИК',
  // ... добавьте свои слова (должны быть из 5 букв)
].map(word => word.toUpperCase());
```

## 📄 Лицензия

Проект создан в образовательных целях.
